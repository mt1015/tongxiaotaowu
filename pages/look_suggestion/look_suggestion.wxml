<!--pages/look_suggestion/look_suggestion.wxml-->
<view class="page">
  <view class="item"
  wx:for="{{List}}"
  wx:key='id'>
  <view class="suggestion">
    <view class="nick">{{item.user_name}}</view>
    <view class="content">{{item.content}}</view>
  </view>
</view>
</view>
